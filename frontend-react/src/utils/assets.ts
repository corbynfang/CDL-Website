// Re-export barrel — import from logoAssets or avatarAssets directly in
// components to keep logo and avatar chunks separate in the Vite build.
export { teamLogoKeys, getTeamLogo } from './logoAssets'
export { avatarNicknames, getPlayerAvatar } from './avatarAssets'
